import Info from '@/app/components/Info';
import './globals.css';

export const metadata = {
	title: 'Portfolio page',
	description: 'Welcome to the my portfolio page.',
};

const Home = () => <Info />;

export default Home;
